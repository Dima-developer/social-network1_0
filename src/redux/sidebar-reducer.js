let initialState = {
    friends: [
        { id: 1, name: "Julia" },
        { id: 2, name: "Arina" },
        { id: 3, name: "Mark" }
    ]
};

const sidebarReducer = (state = initialState, action) => {

    return state;
}

export default sidebarReducer;